<h1>Edit Bookmark</h1>
<%= @bookmark.url %>
<%= form_for [@topic, @bookmark] do |f| %>

<div class="form-group">
  <%= f.label :url %>
  <%= f.text_field :url, class: 'form-control', placeholder: "Enter topic bookmark here" %>
</div>

<%= f.submit "Save", class: 'btn btn-success' %>

<% end %>
